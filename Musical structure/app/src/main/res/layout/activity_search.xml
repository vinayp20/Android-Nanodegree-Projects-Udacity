<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_search"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.android.myapplication4.NowPlaying">

    <TextView
        android:text="This activity displays the music searched by the user in the app. We can navigate from this activity to main activity or now playing to play from favourites or shop activity to shop that song from search."
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textViewSearch" />

    <Button
        android:text="Main Activity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_margin="20dp"
        android:id="@+id/search_main"
        android:textColor="@color/button_text"
        android:background="@color/button_color"/>

    <Button
        android:text="Play NOW"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_margin="20dp"
        android:id="@+id/search_now"
        android:textColor="@color/button_text"
        android:background="@color/button_color"/>

    <Button
        android:text="Shop"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_margin="20dp"
        android:id="@+id/search_shop"
        android:textColor="@color/button_text"
        android:background="@color/button_color"/>>
</LinearLayout>
