<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_now_playing"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.android.myapplication4.NowPlaying">

    <TextView
        android:text="This activity displays the current music being played in the app. We can navigate from this activity to main activity or shop activity. The music can be played using Shuttle which is an open source music player for android."
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textViewNow" />

    <Button
        android:text="Main Activity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_margin="20dp"
        android:id="@+id/now_main"
        android:textColor="@color/button_text"
        android:background="@color/button_color"/>

    <Button
        android:text="Shop"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_margin="20dp"
        android:id="@+id/now_shop"
        android:textColor="@color/button_text"
        android:background="@color/button_color"/>>
</LinearLayout>
